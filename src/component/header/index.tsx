import React from "react";
import { LanguageSwitcher } from "./local-switcher";

const Header = () => {
  return (
    <div>
      Header
      <LanguageSwitcher />
    </div>
  );
};

export default Header;
